<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto">
	<data>
		<variable
			name="viewBinder"
			type="com.png.interview.weather.ui.binder.ForecastFragmentViewBinder" />
	</data>
	<androidx.constraintlayout.widget.ConstraintLayout
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:padding="16dp">

		<TextView
			android:id="@+id/tv_header"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:text="@string/current_weather"
			android:textSize="18sp"
			android:textStyle="bold"
			app:layout_constraintTop_toTopOf="parent"
			app:layout_constraintStart_toStartOf="parent" />

		<!-- TODO: Update this to a proper recycler view and use an adapter -->
		<androidx.appcompat.widget.LinearLayoutCompat
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:orientation="vertical"
			app:visible="@{viewBinder.availableWeatherViewData != null}"
			app:layout_constraintTop_toBottomOf="@id/tv_header"
			>

			<include
				layout="@layout/view_forecast_day_item"
				android:layout_height="wrap_content"
				android:layout_width="match_parent"
				android:layout_marginTop="16dp"
				app:viewData="@{viewBinder.availableWeatherViewData.data.get(0)}" />

			<include
				layout="@layout/view_forecast_day_item"
				android:layout_height="wrap_content"
				android:layout_width="match_parent"
				android:layout_marginTop="16dp"
				app:viewData="@{viewBinder.availableWeatherViewData.data.get(1)}" />

			<include
				layout="@layout/view_forecast_day_item"
				android:layout_height="wrap_content"
				android:layout_width="match_parent"
				android:layout_marginTop="16dp"
				app:viewData="@{viewBinder.availableWeatherViewData.data.get(2)}" />
		</androidx.appcompat.widget.LinearLayoutCompat>
	</androidx.constraintlayout.widget.ConstraintLayout>
</layout>